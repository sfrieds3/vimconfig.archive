My vim config files. Optimized for Python, Perl, common lisp, and Ruby (Rails).

Run the following to clone the repository, init package submodules, and link all the stuff:
$ git clone https://github.com/scwfri/vimconfig.git ~/vimconfig && ~/vimconfig/setvim.sh
